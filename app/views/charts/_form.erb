<h1><%= @chart.id ? "Edit Chart" : "Add Chart"%>
<h3><%= form_for [@chart] do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.submit %></h3>
<% end %>
<br />
<%= link_to "Go Back", charts_path %>