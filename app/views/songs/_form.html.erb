<h1><%= @song.id ? "Edit Song" : "Add Song"%>
<h3> <%= form_for [@song] do |f| %>
<%= f.label :title %>
<%= f.text_field :title %>

<%= f.submit %> </h3>
<% end %>

<br />
<%= link_to 'Go Back', artist_path(@artist) %>